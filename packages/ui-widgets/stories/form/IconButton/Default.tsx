import React from 'react';

import { IconButton, Icons } from '../../../src';

export default () => (
  <div style={{ padding: '2.4rem', display: 'flex', justifyContent: 'center' }}>
    <IconButton>
      <Icons.ArrowLeft />
    </IconButton>
  </div>
);
